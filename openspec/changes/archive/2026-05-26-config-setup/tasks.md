## 1. Update openspec/config.yaml

- [x] 1.1 [SPEC] Add `context` block: tech stack (Java/Quarkus, Vue.js, Rust, PostgreSQL, Keycloak), domain terms (Exam, Sentinel, Proctor, Session, Violation, Alarm), roles (Schüler, Lehrer, Admin), language note (German UI / English code)
- [x] 1.2 [SPEC] Add `rules.proposal`: require FSD section references (§N.M) in each proposal
- [x] 1.3 [SPEC] Add `rules.specs`: require implementation status (`implemented` / `partial` / `not-implemented`) per requirement; scope each spec to one capability
- [x] 1.4 [SPEC] Add `rules.tasks`: require `[GAP]` label for FSD requirements not yet implemented; require `[SPEC]` label for spec-writing-only tasks
