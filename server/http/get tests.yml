info:
  name: get tests
  type: graphql
  seq: 1

graphql:
  method: POST
  url: "{{BASE_URL}}/api/graphql"
  body:
    query: |
      query GetTests {
        tests {
          teacherId
          id
          startTime
          endTime
          title
          pin
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
