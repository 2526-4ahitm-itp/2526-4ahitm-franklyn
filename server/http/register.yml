info:
  name: register
  type: http
  seq: 8

http:
  method: GET
  url: "{{BASE_URL}}/api/register"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
