name: franklyn
variables:
  - name: KLC_CLIENT_SECRET
    value: "{{process.env.KEYCLOAK_SECRET}}"
  - name: KLC_URL
    value: https://auth.htl-leonding.ac.at/realms/htlleonding
    disabled: true
  - name: KLC_PASSWORD
    value: "{{process.env.KEYCLOAK_PASSWORD}}"
  - name: KLC_USERNAME
    value: "{{process.env.KEYCLOAK_USER}}"
  - name: BASE_URL
    value: http://localhost:5050
  - name: KLC_URL
    value: https://auth.htl-leonding.ac.at/realms/franklyn
