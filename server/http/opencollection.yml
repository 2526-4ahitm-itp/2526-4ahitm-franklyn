opencollection: 1.0.0

info:
  name: franklyn
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  auth:
    type: oauth2
    flow: resource_owner_password_credentials
    accessTokenUrl: "{{KLC_URL}}/protocol/openid-connect/token"
    credentials:
      clientId: htlleonding-service
      clientSecret: "{{KLC_CLIENT_SECRET}}"
      placement: basic_auth_header
    resourceOwner:
      username: it220266
      password: "{{KLC_PASSWORD}}"
    scope: openid
    tokenConfig:
      id: access_token
      placement:
        header: Bearer
    settings:
      autoFetchToken: true
      autoRefreshToken: true
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
