info:
  name: healthcheck
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5050/api/q/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
