info:
  name: healthcheck
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/api/q/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
